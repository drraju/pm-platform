# Feature 1.2.6 - Calendar Assignment

## Stage 7.5 Infrastructure Validation

**Branch:** `develop-v1.2`  
**Date:** 2026-07-15  
**Governance:** Engineering Governance v1.0, Addendum v1.1, Addendum v1.2  
**ADR:** ADR-008 (Accepted)  
**Result:** PASS

## Scope

Runtime-only validation of Feature 1.2.6 in Docker with PostgreSQL. No implementation changes were required.

## Results

### 1. Docker

| Check | Result | Evidence |
| --- | --- | --- |
| Backend image builds | PASS | `docker compose build backend frontend` completed successfully |
| Frontend image builds | PASS | `docker compose build backend frontend` completed successfully |
| Compose startup | PASS | `docker compose up -d` completed successfully |
| Backend | PASS | Running and healthy |
| Frontend | PASS | Running and healthy |
| PostgreSQL | PASS | Running and healthy |
| Redis | PASS | Running and healthy |
| MinIO | PASS | Running; Compose does not define a MinIO healthcheck |
| Restart counts | PASS | All five Compose services reported restart count `0` |

### 2. Database migrations

| Check | Result | Evidence |
| --- | --- | --- |
| Fresh database | PASS | Isolated database started from `001_initial_schema.sql`; runtime applied all 23 migrations from 002 through 024 in filename order |
| Upgrade from 023 | PASS | Existing database initially contained migrations 002-023; backend startup applied 024 successfully |
| Migration 024 exactly once | PASS | `schema_migrations` contained one row for `024_v1_2_6_resource_calendar_assignment.sql` in both fresh and upgraded databases |

The isolated fresh-database backend reached healthy status with restart count `0`. The temporary container and database were removed after validation.

### 3. Database constraints

| Check | Result | Evidence |
| --- | --- | --- |
| Nullable `calendar_id` UUID | PASS | `enterprise_resources.calendar_id` is type `uuid` and nullable |
| Foreign key | PASS | References `enterprise_calendars(id)` |
| Delete behavior | PASS | `ON DELETE RESTRICT`; a referenced Calendar deletion raised a foreign-key violation |
| Update behavior | PASS | `ON UPDATE RESTRICT`; a referenced Calendar ID update raised a foreign-key violation |
| Invalid Calendar reference | PASS | Direct invalid assignment raised a foreign-key violation |
| Shared Calendar | PASS | Two Resources were assigned to one Calendar through the API |
| Index | PASS | Partial index `idx_enterprise_resources_calendar` exists on `calendar_id` for live, assigned Resources |
| NULL assignment | PASS | NULL persisted successfully |

### 4. Seed verification

The seed command completed successfully on repeated runs.

| Check | Result | Evidence |
| --- | --- | --- |
| Duplicate permissions | PASS | Zero duplicate permission keys |
| Duplicate roles | PASS | Zero duplicate role names |
| Existing permission definitions unchanged | PASS | Permission checksum remained `083c687a6944eb5acb96247205b13152` before and after another seed run |
| Existing role-permission assignments unchanged | PASS | Role-permission checksum remained `097ae9ea185ba8988df57cb6c1b5f24c` |
| Required ERM permissions | PASS | `resource.read` and `resource.update` remained present |

### 5. Swagger runtime

`/api/docs-json` contains GET, PUT, and DELETE operations at `/resources/{resourceId}/calendar`.

| Check | Result |
| --- | --- |
| Response DTO schema | PASS |
| Nullable UUID fields | PASS (`assignedCalendarId`, `effectiveCalendarId`, `updatedBy`) |
| Nullable string fields | PASS (`calendarName`, `calendarStatus`) |
| UUID request schema | PASS (`calendarId`) |
| Bearer authentication | PASS on all three operations |

### 6. Health

`GET /health` returned HTTP 200 with overall status `ok`, PostgreSQL `up`, and Redis `up`.

### 7. Runtime API

| Scenario | Result | Runtime response |
| --- | --- | --- |
| Null/unassigned retrieval | PASS | 200; nullable fields were NULL and state was `unassigned` |
| Assign Calendar | PASS | 200; assigned Calendar returned |
| Retrieve assignment | PASS | 200; assignment and Calendar details returned |
| Replace assignment | PASS | 200; replacement Calendar returned |
| Clear assignment | PASS | 204; later GET returned `unassigned` |
| Missing Resource | PASS | 404 |
| Missing Calendar | PASS | 404 |
| Invalid Resource UUID | PASS | 400 |
| Invalid Calendar UUID | PASS | 400 |
| Archived Calendar | PASS | 400, not active |
| Soft-deleted Calendar | PASS | 400, soft deleted |
| Explicit NULL assignment payload | PASS | 400; PUT requires a UUID and DELETE is the clear operation |
| Shared Calendar | PASS | Two Resources accepted the same active Calendar |

### 8. PostgreSQL verification

PostgreSQL inspection confirmed migration 024, the foreign key, partial index, RESTRICT update/delete behavior, shared references, rejection of invalid references, and acceptance of NULL assignments.

## Conclusion

Feature 1.2.6 satisfies the Stage 7.5 infrastructure validation objective. No genuine runtime defect was found and no implementation was modified.
